import { StyledFooter } from "./Footer.styled";

const Footer:React.FC=() => {
    return (
        <StyledFooter>
            All Rights Reserved
        </StyledFooter>
    );
}
export default Footer;